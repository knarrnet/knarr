# Release Notes — v0.50.0 "Stabilization"

**Date:** 2026-03-22
**Base:** v0.49.0

---

## Summary

Stabilization sprint with 22 items across 5 tracks. KAD skill store wired into publish and query paths (iterative STORE, FIND_VALUE, provider-record model). Gossip republish flood fixed (fanout→all-peers for scheduled cycles). Commerce integration: BCW payment credit subscriber, query result caching, reconnect routing table population. Security: O(1) pubkey lookup, watchman patches, netting formula alignment, settlement receipt fix. Infrastructure: bootstrap transient join, config validation, WebSocket/HTTP event shape normalization.

## Breaking Changes

None. All changes are additive or fix existing behavior.

---

## Highlights

- **BUG-01:** Gossip republish sends to ALL peers on scheduled cycle (was fanout=3)
- **BUG-03:** Connection timeout/cancellation logged at DEBUG (was ERROR)
- **BUG-04:** `skill.registered` / `skill.removed` bus events now emitted
- **CR-01:** BCW `payment.finalized.*` events credit the ledger
- **CR-02:** Query-learned providers cached locally
- **CR-03:** `reconnect_from_cache()` populates routing table
- **CR-05:** `GET /api/demand` cockpit endpoint
- **SEC-01:** `_resolve_public_key()` O(1) via `get_pubkey_by_node_id()`
- **KAD-01:** DHT skill store with iterative STORE/FIND_VALUE

Panel: 4 devs (Gemini/GPT/Sonnet/Opus), 3 judges + tiebreaker, 5 adversaries. B=9, C=9, D=3 wins.
