# Minimal Profile

This profile creates the smallest DHT-participating Knarr node scaffold.

Files:
- `knarr.toml`: base node and bootstrap settings
- `plugins.toml`: records that the node relies on built-in package plugins

Usage:
```bash
cd $directory
knarr serve
```
